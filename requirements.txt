# OpenML Experiments - Python dependencies
# Tested with Python 3.14

# Core scientific stack
numpy>=1.26
scipy>=1.11
pandas>=2.0

# ML
scikit-learn>=1.3
openml>=0.14

# Interactive notebook (optional - only for 09-experiments-notebook.ipynb)
# VS Code's Jupyter extension uses the installed ipykernel directly;
# standalone JupyterLab/classic notebook needs jupyterlab or notebook.
# nbformat ships with Jupyter; nbclient runs notebooks programmatically.
ipykernel>=6.0
nbformat>=5.0
nbclient>=0.5

# Optional: for Project 3 (Kanungo Ch. 7 PLE on AAPL/SPY)
# Only needed if you want to reproduce the Bayesian MCMC implementation.
# Standard library + numpy + scipy are enough for the from-scratch version.
# matplotlib>=3.8  # only if you want to plot posterior distributions

# Install with:
#     pip install -r requirements.txt
#
# Then reproduce any experiment with:
#     python 01-reproduce-credit-g-knn-sklearn.py
#     python 02-compare-3-algorithms-ai4i.py
#     python 02b-compare-3-algorithms-ai4i-no-leakage.py
#     python 04-hdf-narrow-problem.py
#     python dpmo.py   # runs the self-test
#
# Or open the interactive notebook:
#     code 09-experiments-notebook.ipynb    # then run cells interactively
#     (works in VS Code with the Jupyter extension, or in JupyterLab)
